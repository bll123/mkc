__mkc__

### Contents

- Requirements
- Building
- Testing
- Install

#### Requirements

The files generated by bison and flex are shipped with the source.  A
`make distclean` will remove these files.  bison and flex are not
required if building from the source distribution.

Build Requirements

- C compiler
- libpcre2-posix
- lorder
  For future use. Recommended.
  Linux doesn't ship this, an lorder script is included for Linux.
- bison
- flex
- pkgconf or pkg-config
- /bin/sh touch rm sed echo

Runtime Requirements

- libpcre2-posix
- lorder
  For future use. Recommended.
  Linux doesn't ship this, an lorder script is included for Linux.
- pkgconf or pkg-config

#### Building

The bootstrap process will build _mkc_ three times.

make realclean    # preserves the mkc_bison.[ch] and mkc_lex.[ch] files
make

#### Testing

./tests/runtests.sh

#### Install

PREFIX=target-dir make test-install
