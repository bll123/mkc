[Wiki Home](Home)
<br>[Syntax](Syntax)

[TOC]

### Syntax: Checks

##### Allowed Attributes

Check statements may allow the following attribute statements:

* [alternate](SyntaxAttrAlternate)
* [compiler_flags](SyntaxAttrCompileFlag)
* [header](SyntaxAttrHeader)
* [link_flags](SyntaxAttrLinkFlag)
* [match](SyntaxAttrMatch)
* [name](SyntaxAttrName)
* [negate](SyntaxCheckCompilerFlag)
* [path](SyntaxAttrPath)

##### Setting Required Headers for a Check

The headers for a check may set by appending a statement block, and
using the [header](SyntaxHeader) attribute.

    check_size int64_t {
      header stdint.h;
    }

##### Generated Names

_mkc_ automatically generates names for each check. Each check has an
associated prefix.  The name of the check will replace characters that
are not alphabetic or digits with an underscore.

##### Changing the Name of a Check

_mkc_ automatically generates names for each check. To change the name
of a check, append a statement block and use the [name](SyntaxName)
statement.

    check_size int64_t {
      header stdint.h;
      name HAVE_INT64;
    }

##### Setting Required Flags for a Check

    check_size int64_t {
      compiler_flags '-DANSI';
    }

##### Setting Required Libraries for a Check

    check_size int64_t {
      link_flags '-lintl';
    }

##### Alternates

The [alternate](SyntaxAttrAlternate) is a powerful method to run
checks with alternate settings.

Each alternate attribute block may contain the
[header](SyntaxAttrHeader), [compiler_flags](SyntaxAttrCompileFlag),
or [link_flags](SyntaxAttrLinkFlag) attributes.

For example, the following code checks for the _statfs_ function.  On
Linux, this requires the `sys/vfs.h` header (or `sys/statfs.h`).  On
MacOS and \*BSD, the `sys/mount.h` header should be used.

    check_function statfs {
      alternate {
        # linux
        name _header_sys_vfs;
        header sys/vfs.h;
      }
      alternate {
        # macos, freebsd
        name _header_sys_mount;
        header sys/mount.h;
      }
    }

On success, the _\_function\_statfs_ generated name will be set.

If the first alternate succeeds, the _\_header\_sys\_vfs_ variable
will be set.  If the second alternate succeeds, the
_\_header\_sys\_mount_ variable will be set.

##### Checks

* [check_arg_count](SyntaxCheckArgCount)
* [check_compiler_flag](SyntaxCheckCompilerFlag)
  add_compiler_flag
* [check_constant](SyntaxCheckConstant)
* [check_define](SyntaxCheckDefine)
* [check_function](SyntaxCheckFunction)
* [check_header](SyntaxCheckHeader)
* [check_link_flag](SyntaxCheckLinkFlag)
  add_link_flag
* [check_package](SyntaxCheckPackage)
* [check_size](SyntaxCheckSize)
* [check_shell_extract](SyntaxCheckShell)
* [check_struct_member](SyntaxCheckStructMember)
* [check_type](SyntaxCheckType)

[Wiki Home](Home)
<br>[Syntax](Syntax)
